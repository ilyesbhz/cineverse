{"ast":null,"code":"/* import { useEffect, useRef } from 'react';\nimport * as THREE from 'three';\n\nexport default function HeroThreeScene() {\n  const mountRef = useRef(null);\n\n  useEffect(() => {\n    const mount = mountRef.current;\n    if (!mount) return;\n\n    const W = mount.clientWidth;\n    const H = mount.clientHeight;\n\n    const scene = new THREE.Scene();\n    const camera = new THREE.PerspectiveCamera(60, W / H, 0.1, 100);\n    camera.position.set(0, 0, 8);\n\n    const renderer = new THREE.WebGLRenderer({ alpha: true, antialias: true });\n    renderer.setSize(W, H);\n    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));\n    renderer.setClearColor(0x000000, 0);\n    mount.appendChild(renderer.domElement);\n\n    // Ambient light\n    scene.add(new THREE.AmbientLight(0xffffff, 0.3));\n    const redLight = new THREE.PointLight(0xe50914, 3, 20);\n    redLight.position.set(-5, 3, 5);\n    scene.add(redLight);\n    const blueLight = new THREE.PointLight(0x4466ff, 2, 20);\n    blueLight.position.set(5, -3, 5);\n    scene.add(blueLight);\n\n    const objects = [];\n\n    // Create torus rings (film reel aesthetic)\n    for (let i = 0; i < 3; i++) {\n      const geo = new THREE.TorusGeometry(1.5 + i * 0.6, 0.04, 8, 64);\n      const mat = new THREE.MeshStandardMaterial({\n        color: i === 0 ? 0xe50914 : i === 1 ? 0x4466ff : 0xffffff,\n        emissive: i === 0 ? 0x660000 : i === 1 ? 0x002244 : 0x111111,\n        metalness: 0.8, roughness: 0.2, transparent: true, opacity: 0.7\n      });\n      const torus = new THREE.Mesh(geo, mat);\n      torus.rotation.x = Math.PI / 4 + i * 0.3;\n      torus.rotation.y = i * 0.5;\n      scene.add(torus);\n      objects.push({ mesh: torus, speed: 0.003 + i * 0.002, axis: i % 2 === 0 ? 'x' : 'y' });\n    }\n\n    // Central glowing sphere\n    const sphereGeo = new THREE.SphereGeometry(0.5, 32, 32);\n    const sphereMat = new THREE.MeshStandardMaterial({\n      color: 0xe50914, emissive: 0xe50914, emissiveIntensity: 0.8,\n      metalness: 1, roughness: 0, transparent: true, opacity: 0.9\n    });\n    const sphere = new THREE.Mesh(sphereGeo, sphereMat);\n    scene.add(sphere);\n\n    // Floating cubes\n    for (let i = 0; i < 15; i++) {\n      const size = 0.05 + Math.random() * 0.15;\n      const geo = new THREE.BoxGeometry(size, size, size);\n      const mat = new THREE.MeshStandardMaterial({\n        color: Math.random() > 0.5 ? 0xe50914 : 0x4466ff,\n        emissive: Math.random() > 0.5 ? 0x440000 : 0x001133,\n        metalness: 0.9, roughness: 0.1, transparent: true, opacity: 0.6\n      });\n      const cube = new THREE.Mesh(geo, mat);\n      cube.position.set((Math.random() - 0.5) * 12, (Math.random() - 0.5) * 8, (Math.random() - 0.5) * 4);\n      cube.userData = {\n        vx: (Math.random() - 0.5) * 0.005,\n        vy: (Math.random() - 0.5) * 0.005,\n        rx: Math.random() * 0.02,\n        ry: Math.random() * 0.02\n      };\n      scene.add(cube);\n      objects.push({ mesh: cube, isCube: true });\n    }\n\n    // Particle trail\n    const trailGeo = new THREE.BufferGeometry();\n    const count = 500;\n    const pos = new Float32Array(count * 3);\n    for (let i = 0; i < count; i++) {\n      pos[i * 3] = (Math.random() - 0.5) * 20;\n      pos[i * 3 + 1] = (Math.random() - 0.5) * 12;\n      pos[i * 3 + 2] = (Math.random() - 0.5) * 8 - 2;\n    }\n    trailGeo.setAttribute('position', new THREE.BufferAttribute(pos, 3));\n    const trailMat = new THREE.PointsMaterial({ size: 0.03, color: 0xe50914, transparent: true, opacity: 0.4 });\n    const trail = new THREE.Points(trailGeo, trailMat);\n    scene.add(trail);\n\n    // Mouse interaction\n    const mouse = { x: 0, y: 0 };\n    const handleMouse = (e) => {\n      mouse.x = (e.clientX / window.innerWidth) * 2 - 1;\n      mouse.y = -(e.clientY / window.innerHeight) * 2 + 1;\n    };\n    window.addEventListener('mousemove', handleMouse);\n\n    let t = 0;\n    let animId;\n    const animate = () => {\n      animId = requestAnimationFrame(animate);\n      t += 0.01;\n\n      // Rotate rings\n      objects.forEach(obj => {\n        if (!obj.isCube) {\n          obj.mesh.rotation.x += obj.speed * 0.8;\n          obj.mesh.rotation.y += obj.speed;\n        } else {\n          obj.mesh.position.x += obj.mesh.userData.vx;\n          obj.mesh.position.y += obj.mesh.userData.vy;\n          obj.mesh.rotation.x += obj.mesh.userData.rx;\n          obj.mesh.rotation.y += obj.mesh.userData.ry;\n          if (Math.abs(obj.mesh.position.x) > 7) obj.mesh.userData.vx *= -1;\n          if (Math.abs(obj.mesh.position.y) > 5) obj.mesh.userData.vy *= -1;\n        }\n      });\n\n      // Pulse sphere\n      const scale = 1 + Math.sin(t * 2) * 0.15;\n      sphere.scale.set(scale, scale, scale);\n      sphereMat.emissiveIntensity = 0.5 + Math.sin(t * 3) * 0.3;\n\n      // Mouse parallax\n      camera.position.x += (mouse.x * 0.5 - camera.position.x) * 0.05;\n      camera.position.y += (mouse.y * 0.3 - camera.position.y) * 0.05;\n      camera.lookAt(0, 0, 0);\n\n      // Animate lights\n      redLight.position.x = Math.sin(t * 0.5) * 5;\n      blueLight.position.x = -Math.sin(t * 0.5) * 5;\n\n      // Scroll trail\n      const p = trailGeo.attributes.position.array;\n      for (let i = 0; i < count; i++) {\n        p[i * 3 + 2] += 0.01;\n        if (p[i * 3 + 2] > 5) p[i * 3 + 2] = -8;\n      }\n      trailGeo.attributes.position.needsUpdate = true;\n\n      renderer.render(scene, camera);\n    };\n    animate();\n\n    const handleResize = () => {\n      if (!mount) return;\n      camera.aspect = mount.clientWidth / mount.clientHeight;\n      camera.updateProjectionMatrix();\n      renderer.setSize(mount.clientWidth, mount.clientHeight);\n    };\n    window.addEventListener('resize', handleResize);\n\n    return () => {\n      cancelAnimationFrame(animId);\n      window.removeEventListener('mousemove', handleMouse);\n      window.removeEventListener('resize', handleResize);\n      renderer.dispose();\n      if (mount.contains(renderer.domElement)) mount.removeChild(renderer.domElement);\n    };\n  }, []);\n\n  return <div ref={mountRef} style={{ width: '100%', height: '100%' }} />;\n} */","map":{"version":3,"names":[],"sources":["C:/home/claude/cineverse/client/src/three/HeroThreeScene.js"],"sourcesContent":["/* import { useEffect, useRef } from 'react';\nimport * as THREE from 'three';\n\nexport default function HeroThreeScene() {\n  const mountRef = useRef(null);\n\n  useEffect(() => {\n    const mount = mountRef.current;\n    if (!mount) return;\n\n    const W = mount.clientWidth;\n    const H = mount.clientHeight;\n\n    const scene = new THREE.Scene();\n    const camera = new THREE.PerspectiveCamera(60, W / H, 0.1, 100);\n    camera.position.set(0, 0, 8);\n\n    const renderer = new THREE.WebGLRenderer({ alpha: true, antialias: true });\n    renderer.setSize(W, H);\n    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));\n    renderer.setClearColor(0x000000, 0);\n    mount.appendChild(renderer.domElement);\n\n    // Ambient light\n    scene.add(new THREE.AmbientLight(0xffffff, 0.3));\n    const redLight = new THREE.PointLight(0xe50914, 3, 20);\n    redLight.position.set(-5, 3, 5);\n    scene.add(redLight);\n    const blueLight = new THREE.PointLight(0x4466ff, 2, 20);\n    blueLight.position.set(5, -3, 5);\n    scene.add(blueLight);\n\n    const objects = [];\n\n    // Create torus rings (film reel aesthetic)\n    for (let i = 0; i < 3; i++) {\n      const geo = new THREE.TorusGeometry(1.5 + i * 0.6, 0.04, 8, 64);\n      const mat = new THREE.MeshStandardMaterial({\n        color: i === 0 ? 0xe50914 : i === 1 ? 0x4466ff : 0xffffff,\n        emissive: i === 0 ? 0x660000 : i === 1 ? 0x002244 : 0x111111,\n        metalness: 0.8, roughness: 0.2, transparent: true, opacity: 0.7\n      });\n      const torus = new THREE.Mesh(geo, mat);\n      torus.rotation.x = Math.PI / 4 + i * 0.3;\n      torus.rotation.y = i * 0.5;\n      scene.add(torus);\n      objects.push({ mesh: torus, speed: 0.003 + i * 0.002, axis: i % 2 === 0 ? 'x' : 'y' });\n    }\n\n    // Central glowing sphere\n    const sphereGeo = new THREE.SphereGeometry(0.5, 32, 32);\n    const sphereMat = new THREE.MeshStandardMaterial({\n      color: 0xe50914, emissive: 0xe50914, emissiveIntensity: 0.8,\n      metalness: 1, roughness: 0, transparent: true, opacity: 0.9\n    });\n    const sphere = new THREE.Mesh(sphereGeo, sphereMat);\n    scene.add(sphere);\n\n    // Floating cubes\n    for (let i = 0; i < 15; i++) {\n      const size = 0.05 + Math.random() * 0.15;\n      const geo = new THREE.BoxGeometry(size, size, size);\n      const mat = new THREE.MeshStandardMaterial({\n        color: Math.random() > 0.5 ? 0xe50914 : 0x4466ff,\n        emissive: Math.random() > 0.5 ? 0x440000 : 0x001133,\n        metalness: 0.9, roughness: 0.1, transparent: true, opacity: 0.6\n      });\n      const cube = new THREE.Mesh(geo, mat);\n      cube.position.set((Math.random() - 0.5) * 12, (Math.random() - 0.5) * 8, (Math.random() - 0.5) * 4);\n      cube.userData = {\n        vx: (Math.random() - 0.5) * 0.005,\n        vy: (Math.random() - 0.5) * 0.005,\n        rx: Math.random() * 0.02,\n        ry: Math.random() * 0.02\n      };\n      scene.add(cube);\n      objects.push({ mesh: cube, isCube: true });\n    }\n\n    // Particle trail\n    const trailGeo = new THREE.BufferGeometry();\n    const count = 500;\n    const pos = new Float32Array(count * 3);\n    for (let i = 0; i < count; i++) {\n      pos[i * 3] = (Math.random() - 0.5) * 20;\n      pos[i * 3 + 1] = (Math.random() - 0.5) * 12;\n      pos[i * 3 + 2] = (Math.random() - 0.5) * 8 - 2;\n    }\n    trailGeo.setAttribute('position', new THREE.BufferAttribute(pos, 3));\n    const trailMat = new THREE.PointsMaterial({ size: 0.03, color: 0xe50914, transparent: true, opacity: 0.4 });\n    const trail = new THREE.Points(trailGeo, trailMat);\n    scene.add(trail);\n\n    // Mouse interaction\n    const mouse = { x: 0, y: 0 };\n    const handleMouse = (e) => {\n      mouse.x = (e.clientX / window.innerWidth) * 2 - 1;\n      mouse.y = -(e.clientY / window.innerHeight) * 2 + 1;\n    };\n    window.addEventListener('mousemove', handleMouse);\n\n    let t = 0;\n    let animId;\n    const animate = () => {\n      animId = requestAnimationFrame(animate);\n      t += 0.01;\n\n      // Rotate rings\n      objects.forEach(obj => {\n        if (!obj.isCube) {\n          obj.mesh.rotation.x += obj.speed * 0.8;\n          obj.mesh.rotation.y += obj.speed;\n        } else {\n          obj.mesh.position.x += obj.mesh.userData.vx;\n          obj.mesh.position.y += obj.mesh.userData.vy;\n          obj.mesh.rotation.x += obj.mesh.userData.rx;\n          obj.mesh.rotation.y += obj.mesh.userData.ry;\n          if (Math.abs(obj.mesh.position.x) > 7) obj.mesh.userData.vx *= -1;\n          if (Math.abs(obj.mesh.position.y) > 5) obj.mesh.userData.vy *= -1;\n        }\n      });\n\n      // Pulse sphere\n      const scale = 1 + Math.sin(t * 2) * 0.15;\n      sphere.scale.set(scale, scale, scale);\n      sphereMat.emissiveIntensity = 0.5 + Math.sin(t * 3) * 0.3;\n\n      // Mouse parallax\n      camera.position.x += (mouse.x * 0.5 - camera.position.x) * 0.05;\n      camera.position.y += (mouse.y * 0.3 - camera.position.y) * 0.05;\n      camera.lookAt(0, 0, 0);\n\n      // Animate lights\n      redLight.position.x = Math.sin(t * 0.5) * 5;\n      blueLight.position.x = -Math.sin(t * 0.5) * 5;\n\n      // Scroll trail\n      const p = trailGeo.attributes.position.array;\n      for (let i = 0; i < count; i++) {\n        p[i * 3 + 2] += 0.01;\n        if (p[i * 3 + 2] > 5) p[i * 3 + 2] = -8;\n      }\n      trailGeo.attributes.position.needsUpdate = true;\n\n      renderer.render(scene, camera);\n    };\n    animate();\n\n    const handleResize = () => {\n      if (!mount) return;\n      camera.aspect = mount.clientWidth / mount.clientHeight;\n      camera.updateProjectionMatrix();\n      renderer.setSize(mount.clientWidth, mount.clientHeight);\n    };\n    window.addEventListener('resize', handleResize);\n\n    return () => {\n      cancelAnimationFrame(animId);\n      window.removeEventListener('mousemove', handleMouse);\n      window.removeEventListener('resize', handleResize);\n      renderer.dispose();\n      if (mount.contains(renderer.domElement)) mount.removeChild(renderer.domElement);\n    };\n  }, []);\n\n  return <div ref={mountRef} style={{ width: '100%', height: '100%' }} />;\n} */"],"mappings":"AAAA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA;AACA","ignoreList":[]},"metadata":{},"sourceType":"module","externalDependencies":[]}